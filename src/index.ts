import * as Bitwise from './bitwise'
import * as Typing from './typing'
import * as Generate from './generate'

export namespace Numeric {
  export const bitwiset = Bitwise
  export const generate = Generate
  export const typing = Typing
}
